Build regular expression, to get the desired output, replacing the regexp part in the following ruby code, meaning the code in between the //:

#!/usr/bin/env ruby
puts ARGV[0].scan(/insert regexp here/).join
